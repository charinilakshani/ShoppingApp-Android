<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rettrrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrontView.CategoryActivity"></activity>
        <activity android:name=".FrontView.HomeActivity" />
        <activity android:name=".FrontView.SearchActivity" />
        <activity android:name=".FrontView.ViewProductActivity" />
        <activity android:name=".FrontView.RegisterActivity" />
        <activity android:name=".FrontView.LoginActivity" />
        <activity android:name=".FrontView.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>